# Inbox Folder

Drop raw PRDs, meeting notes, brainstorms, and requirement documents here.

Run `/ingest` to compile them into strict specifications.

Files will be moved to `docs/archive/` after processing.
